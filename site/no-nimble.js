// Deprecated compatibility export. Component exclusion is now implemented by
// the CSS itself, so this import no longer changes stylesheets or the document.
console.warn(
  '[nimble.css] `@leftium/nimble.css/no-nimble` is deprecated and no longer needed. ' +
  'Remove this import before a future release removes the compatibility export.'
)
